version: '3.8'
services:
  worker1:
#    image: map-server
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8080"

  worker2:
#    image: map-server
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"